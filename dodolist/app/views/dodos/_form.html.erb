<%= form_for(@dodo) do |f| %>
  <% if @dodo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dodo.errors.count, "error") %> prohibited this dodo from being saved:</h2>

      <ul>
      <% @dodo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
